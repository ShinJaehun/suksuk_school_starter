#!/bin/bash -e

# Prepare the database before either supported web server command.
if [[ ( "${1}" == "./bin/rails" && "${2}" == "server" ) ||
      ( "${1}" == "bundle" && "${2}" == "exec" && "${3}" == "puma" ) ]]; then
  ./bin/rails db:prepare
fi

exec "${@}"
